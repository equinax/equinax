experiment_id: "exp-20260217-008"
strategy: dragon
created_at: "2026-02-17T10:30:00+08:00"
parent_version: "22"
parent_experiment: "exp-20260217-007"

hypothesis: |
  Stack another strong unused signal: vol_ramp_5v20 (d=0.181, volume 5d/20d
  ratio) at weight 0.03. Measures short-term volume acceleration — higher
  values mean volume is ramping up (buying interest increasing). Total
  positive: 0.87→0.90 (at bound limit). Building on Round 7's success
  of adding up_volume_ratio.

changes:
  - path: "scoring.positive_factors"
    action: "add"
    signal: "vol_ramp_component"
    source: "vol_ramp_5v20"
    transform: "col.clip(0.0, 1.0) * 100"
    fill_null: 0.0
    weight: 0.03

backtest_params:
  dates: 25
  seed: 42
  tabs: ["dragon"]

results:
  win_rate: 47.4
  avg_return: 0.36
  profit_loss_ratio: 1.97
  vs_baseline:
    win_rate_delta: -6.5
    avg_return_delta: -1.96

decision: revert
decision_reason: |
  WR=47.4% (-9.2pp from Round 7), AR=0.36%. Adding vol_ramp_5v20 hurt badly.
  Vol ramp measures volume acceleration which may not discriminate well for
  top-4 selection — stocks hitting limit-up already have high volume, and
  additional volume acceleration signal may pick volatile noise stocks.
  Revert to Round 7 config (up_volume_ratio only).
