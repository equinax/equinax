experiment_id: "exp-20260217-014"
strategy: dragon
created_at: "2026-02-17T10:30:00+08:00"
parent_version: "22"
parent_experiment: "exp-20260217-007"

hypothesis: |
  Add turnover_change_20d (d=0.177) at minimal weight 0.02 (minimum allowed).
  Measures 5d/20d turnover acceleration — higher means recent trading activity
  is ramping up. Treatment mean=0.1055 vs control=0.0385. Used minimal weight
  to act as pure tie-breaker. Total positive: 0.87→0.89.

changes:
  - path: "scoring.positive_factors"
    action: "add"
    signal: "turnover_change_component"
    source: "turnover_change_20d"
    transform: "(col.clip(-0.5, 0.5) + 0.5) * 100"
    fill_null: 50.0
    weight: 0.02

backtest_params:
  dates: 25
  seed: 42
  tabs: ["dragon"]

results:
  win_rate: 52.6
  avg_return: 2.01
  profit_loss_ratio: 3.52
  vs_baseline:
    win_rate_delta: -4.0
    avg_return_delta: -0.35

decision: revert
decision_reason: |
  WR=52.6% (-4.0pp). Even at minimum weight 0.02, adding turnover change
  degrades performance. Turnover acceleration likely overlaps with
  volume-related factors already in the formula (volume_consistency,
  volume_buildup_quality, up_volume_ratio). The formula already has
  4 volume-related signals — adding a 5th creates noise.

  META-INSIGHT: After R7-R14, it appears the formula is near a LOCAL
  OPTIMUM. Every perturbation (add factor, remove penalty, reweight)
  results in WR ~47-53% vs baseline 56.6%. The only improvement was
  R7's up_volume_ratio at exactly w=0.05. Further config-only
  improvements may require a fundamentally different hypothesis type.
